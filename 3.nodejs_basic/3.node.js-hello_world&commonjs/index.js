const calc = require('./calc');

console.log(calc.add(1, 2));
console.log(calc.add(4, 5));
console.log(calc.sub(10, 2));

// node ./3.nodejs_basic/index.js 로 실행하여 결과값을 얻는다.

// node.js를 이용하면 모듈 단위로 만들어 사용할 수 있다.
